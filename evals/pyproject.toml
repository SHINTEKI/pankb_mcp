[project]
name = "pankb-evals"
version = "0.1.0"
description = "Golden-set tool-selection evals for the PanKB agent."
requires-python = ">=3.12"
dependencies = [
    "ai-client",
    "pyyaml>=6.0",
    "arize-phoenix>=7.0",
    "pandas>=2.0",
]

[tool.uv.sources]
ai-client = { path = "../ai_client" }
